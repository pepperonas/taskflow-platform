# Fast deployment - use pre-built artifacts
FROM nginx:alpine

# Copy pre-built static files
COPY build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
